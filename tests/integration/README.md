# Integration scripts — run manually against the real NAS / live audio files.
# These are NOT pytest tests and are excluded from `pytest` by pytest.ini.
#
# Usage:
#   source .venv/bin/activate
#   python tests/integration/test_analyze_local.py
#   python tests/integration/test_discovery_live.py
